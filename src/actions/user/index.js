import axios from "axios";
import * as t from "./types";

export const authUser = async ({ username, password }) => dipsatch => {
  console.log("username", username);
  console.log("password", password);
};
